spring:
  config:
    activate:
      on-profile: local
  graphql:
    graphiql:
      enabled: true
  application:
    name: producer-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8181

